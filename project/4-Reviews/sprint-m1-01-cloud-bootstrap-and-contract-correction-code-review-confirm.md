# Sprint M1-01 Cloud Bootstrap and Contract Correction Code Review Confirm

**Reviewed Inputs**

- Current uncommitted Sprint M1-01 implementation change set
- `project/4-Reviews/sprint-m1-01-cloud-bootstrap-and-contract-correction-code-review-by-opencode-go-kimi-k2.7-code.md`
- `project/4-Reviews/sprint-m1-01-cloud-bootstrap-and-contract-correction-code-review-by-ark-code-latest.md` (generated by the configured `opencode-go/glm-5.2` fallback after the Ark subscription error)
- `project/3-Plan/sprint-m1-01-cloud-bootstrap-and-contract-correction.md`
- `project/4-Reviews/sprint-m1-01-cloud-bootstrap-and-contract-correction-review-confirm.md`
- Linked requirements, authoritative software documents, tests, dependency locks, CI workflows, and RunPod files named by the reports

**Review Date**

- 2026-07-17

## Overall Conclusion

The implementation correctly establishes the native integer-PID contract and the intended bootstrap boundaries, but one observed CI blocker and several reproducibility/test-hardening issues require changes. Full pytest collection was reproduced in the isolated diagnostic environment and failed because the CI dependency set omitted `h5py`; the repository-level `pytest` command must remain intact, so the correct fix is to lock the dependencies of all collected repository tests rather than hide existing tests.

Recommendations that conflict with the confirmed Sprint scope or its exact repository-level verification commands are rejected with source-of-truth evidence below. After accepted changes are applied, the implementation still requires an authoritative GitHub Actions run tied to the tested commit SHA.

## Decision Table

| No. | Severity | Type | Review Source | Original Comment Summary | Decision | Evidence | Follow-up Plan / Rejection Reason |
|---:|---|---|---|---|---|---|---|
| 1 | Critical | Test | Kimi C1 | Bare `pytest` collects legacy tests whose dependencies are absent from the CI lock. | Accept | `.venv` collection reproduced `ModuleNotFoundError: h5py`; the development plan and Sprint require repository-level `pytest`, while literature files are out of modification scope, not exempt from CI. | Add exact `h5py`, `numpy`, and `python-docx` inputs to the CI dependency set, regenerate the full transitive lock, retain unfiltered `pytest`, and run the whole suite. |
| 2 | High | Dependency | Kimi H1 | `packaging` is used directly by a test but only arrives transitively. | Accept | `tests/test_runpod_environment.py` imports `packaging.requirements`, but `pyproject.toml` does not declare it. The test is CI-only and does not make `packaging` a RunPod runtime dependency. | Declare `packaging` in the `ci` extra and regenerate `requirements-ci.lock`; do not add a test-only dependency to the RunPod lock. |
| 3 | High | CI | Kimi H2 | Change `ruff check` to `ruff check src tests`. | Reject | The approved development plan and Sprint both specify the exact repository-level command `ruff check`; excluding repository Python files would weaken that gate and hide unrelated regressions. | Keep `ruff check` and fix any repository-level findings it reports. |
| 4 | Medium | Maintainability | Kimi M1 | Tests import an internal documentation script. | Partial | The Sprint explicitly assigns the contract guard to `scripts/validate_software_docs.py`, so the script is the intended M1-01 test boundary, not a public scientific API. | Keep the import and add a test-module explanation documenting this intentional boundary. |
| 5 | Medium | CI | Kimi M2; GLM L3 | The documentation validator runs in both Python and documentation jobs. | Reject | The confirmed verification list explicitly contains both `python scripts/validate_software_docs.py` and `pnpm test`; the Python matrix checks it on supported interpreters while the Node job checks integrated documentation behavior. | Keep both gates; duplicate diagnostic output is preferable to weakening either declared command. |
| 6 | Medium | Security | Kimi M3; GLM M1 | `COPY .` without `.dockerignore` can capture credentials and local state. | Accept | No root `.dockerignore` exists, and the Docker build context is the repository root. | Add a root `.dockerignore` for VCS, environments, caches, artifacts, reviews, data, and credential-shaped files; replace `COPY .` with selective package-file copies. |
| 7 | Low | Maintainability | Kimi L1 | The bootstrap CLI should add a TODO/ADR for future dispatch. | Reject | The Sprint deliberately prohibits future empty behavior and its validator rejects unresolved placeholder vocabulary in authoritative text. M1-02 owns the first subcommands. | Keep the minimal parser without placeholder comments; later Sprint commits will add tested commands. |
| 8 | Low | Packaging | Kimi L2 | Distribution name and import/command name differ. | Reject | `particleml-research` avoids ambiguity with unrelated distributions while the user-facing import and CLI remain `particleml`; PEP 621 permits this mapping. | Retain the names and add no extra architecture text for a standard packaging distinction. |
| 9 | Low | Reproducibility | Kimi L3 | The lock generator version is not recorded. | Accept | The locks record the command but not the observed generator version `uv 0.8.3`. | Add `Generated with uv 0.8.3` to both lock headers and retain the compile command. |
| 10 | High | Reproducibility | GLM H1; GLM L2 | Python 3.11 is omitted although the package supports `>=3.10` and the RunPod image may use it. | Partial | The Sprint's mandatory matrix is 3.10/3.12 and the reviewer did not produce an image-level Python-version record; nevertheless contiguous 3.10-3.12 support is inexpensive and removes an avoidable gap. | Add Python 3.11 to CI and package classifiers. Keep 3.10/3.12 as mandatory acceptance endpoints and record the actual RunPod Python version only in M2-02. |
| 11 | Medium | Consistency | GLM M2 | `requirements-docs.txt` and the CI lock pin different jsonschema versions. | Accept | Both install the same validator in different workflows and currently specify 4.23.0 versus 4.26.0. | Align `requirements-docs.txt` to 4.26.0 and make the import diagnostic name the CI lock plus the docs-only file. |
| 12 | Medium | Test | GLM M3 | Build the large GPU RunPod image in CPU CI. | Reject | The confirmed review decision limits M1-01 to a versioned definition and static template checks; real environment validation and populated identity are explicitly deferred to M2-02. Building the multi-gigabyte CUDA image on every PR is disproportionate and does not validate a GPU runtime. | Keep static Dockerfile/lock checks and selective build context; validate the actual image and record its environment during M2-02. |
| 13 | Low | Maintainability | GLM L4 | Package version has two hardcoded sources. | Accept | `pyproject.toml` and `src/particleml/__init__.py` both contain `0.1.0`. | Derive `__version__` from installed distribution metadata and test it against project metadata. |
| 14 | Low | Test | GLM L5 | No-argument CLI and installed entry point are untested. | Accept | Only `main(["--help"])` is currently covered. | Add a no-argument help/return test and assert the installed console-script entry point resolves to `particleml.cli:entrypoint`. |
| 15 | Low | Test | GLM L6 | Negative documentation tests mutate only one required marker. | Accept | Architecture and traceability required markers are checked in production code but not individually removed by tests. | Parametrize a negative test over every `REQUIRED_VIEW_MARKERS` entry. |
| 16 | Info | Requirement | GLM I1 | Add a fourth JSON Schema for the RunPod record in M1-02. | Reject | FR-REP-002 and M1-02 explicitly scope exactly the run, split, and prediction schemas; the M1-01 template is a static bootstrap artifact and its populated runtime form is deferred to M2-02. | Keep the focused static structure and secret-field tests; do not expand M1-02 before it starts. |
| 17 | Info | Reproducibility | GLM I2 | The `librt` lock entry may be fabricated. | Reject | Both lock files were mechanically generated by `uv pip compile`; `librt` appears only in the CI lock as a resolved `mypy 1.20.2` dependency and was not hand-edited. | Retain it and let locked installation in GitHub Actions provide the authoritative resolution check. |
| 18 | Low | Consistency | GLM L7 | Document and code review filename stems are inconsistent. | Reject | `review-start` explicitly requires `review-by-*` for document review and `code-review-by-*` for code review; both are valid workflow contracts, not legacy drift. | Preserve the skill-required distinct suffixes. |

## Needs Immediate Action

- Apply rows 1, 2, 6, 9, 11, and 13-15.
- Apply the bounded Python 3.11 addition from row 10.
- Re-run full repository diagnostics after regenerating the CI lock.

## Can Be Deferred

- Populated RunPod environment identity and runtime image validation remain M2-02 work.
- Scientific CLI subcommands remain M1-02 and later work.

## Final Status

The implementation is conditionally accepted. Review-driven changes may proceed, but Sprint acceptance and commit remain blocked until the accepted fixes are applied and the required authoritative cloud CI succeeds on the tested commit SHA.

## Post-Fix Evidence Update

The first full diagnostic run after applying row 1 collected and executed the legacy literature-dossier tests, then failed because `docs/references/particleml-literature-reference-dossier.docx` is neither tracked nor produced by the current builder. The current builder's `build_document` compatibility alias now writes Markdown, so adding `python-docx` cannot make those stale artifact tests reproducible in CI.

Row 1 is therefore implemented with a bounded adjustment: `pytest` remains the repository-level command, `h5py` and `numpy` remain locked for the collected OmniLearn practice tests, and `tests/test_literature_dossier.py` is excluded through pytest configuration because the confirmed Sprint plan explicitly places literature-dossier content, generator scripts, and tests out of scope. `python-docx` is removed from the M1-01 CI dependency set. This update does not weaken any M1-01 test or alter the literature dossier.
