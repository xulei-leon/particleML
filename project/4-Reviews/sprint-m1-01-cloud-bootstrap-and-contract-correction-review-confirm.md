# Sprint M1-01 Cloud Bootstrap and Contract Correction Review Confirm

**Reviewed Inputs**

- `project/3-Plan/sprint-m1-01-cloud-bootstrap-and-contract-correction.md`
- `project/4-Reviews/sprint-m1-01-cloud-bootstrap-and-contract-correction-review-by-opencode-go-kimi-k2.7-code.md`
- `project/4-Reviews/sprint-m1-01-cloud-bootstrap-and-contract-correction-review-by-ark-code-latest.md` (generated by the configured `opencode-go/glm-5.2` fallback after the Ark subscription error)
- `project/1-Requirement/FR-DATA-005-nested-a-d-views.md`
- `project/1-Requirement/FR-MODEL-003-configuration-specific-adapters.md`
- `project/1-Requirement/FR-REP-002-machine-readable-validation.md`
- `project/3-Plan/particleml-development-plan.md`
- `docs/software/requirements.md`
- `docs/software/architecture.md`
- `docs/software/specification.md`
- `docs/software/traceability-matrix.md`
- `docs/research/research-plan.md`
- `scripts/validate_software_docs.py`
- `.github/workflows/docs.yml`
- `docs/.vitepress/config.mjs`
- `tests/docs-config.test.mjs`

**Review Date**

- 2026-07-17

## Overall Conclusion

The reviews correctly identify blocking ambiguity in the planned integer-PID contract, missing validator scope, and a mismatch between GitHub Actions verification and real RunPod identity capture. The Sprint is not acceptable as written, but it can proceed after the accepted and partially accepted plan corrections below are applied.

The VitePress configuration finding is rejected because the configuration and its test are present and tracked. Exact duplicate comments from the two reports are represented once below; distinct recommendations are retained separately.

## Decision Table

| No. | Severity | Type | Review Source | Original Comment Summary | Decision | Evidence | Follow-up Plan / Rejection Reason |
|---:|---|---|---|---|---|---|---|
| 1 | Critical | Correctness | Kimi C1; GLM H2 | The specification still defines one-hot PID views with C=11 and D=15. | Accept | `docs/software/specification.md` §4.3 conflicts with FR-DATA-005, FR-MODEL-003, and development-plan Phase 0. | Amend the Sprint to require native integer-PID view dimensions A=4, B=5, C=6, D=10; remove unknown-bit and one-hot materialization language; and implement those document corrections in this Sprint. |
| 2 | High | Correctness | Kimi H1; GLM H1 | The architecture still says that the project adapter converts PID to a frozen one-hot representation. | Accept | `docs/software/architecture.md` §5.4 conflicts with the native `--use-pid --pid_idx 4` boundary required by the linked FRs. | Require and implement architecture wording that passes the integer PID column to OmniLearned and does not materialize a project-owned one-hot block. |
| 3 | High | Correctness | Kimi H2; GLM M1 | Canonical storage places charge before PID, while the OmniLearned view contract requires PID at index 4. | Accept | The canonical order in architecture §5.2/specification §3.3 is distinct from the linked FR command contract. | Add an exact A-D ordered-field and argv table to the Sprint and specification, documenting that view construction reorders canonical fields to kinematics, PID, charge, then D fields. |
| 4 | Critical | Requirement | Kimi M1; GLM C1 | The documentation validator is not listed as a modified file and cannot detect a one-hot contract regression. | Accept | `scripts/validate_software_docs.py` currently has no one-hot, dimension, or OmniLearned flag assertion although the Sprint acceptance requires one. | Add the validator to the file table and implement regression checks for forbidden materialized one-hot language, exact A-D dimensions/order, and exact per-configuration flags. |
| 5 | Medium | Risk | Kimi M2 | The VitePress configuration is missing, so `pnpm test` and `pnpm docs:build` will fail. | Reject | `docs/.vitepress/config.mjs` and `tests/docs-config.test.mjs` both exist and are tracked by Git; `package.json` already defines the VitePress commands. | No file-creation work is required. The existing checks remain in CI and will detect an actual regression. |
| 6 | Medium | Clarity | Kimi M3 | RunPod deliverables are described only at directory level. | Accept | The plan names `containers/runpod/` but no concrete image, lock, or record files. | Enumerate `containers/runpod/Dockerfile`, `containers/runpod/requirements.lock`, and `containers/runpod/environment-record.template.json`, plus their focused tests. |
| 7 | Low | Documentation | Kimi L1 | Resolving `DESIGN_DIR` to `docs/software` may confuse later workflows. | Partial | The Sprint already records why `project/2-Design` is absent, and the authoritative suite itself declares `docs/software` as its home. | Clarify in the workflow configuration that `docs/software` is the authoritative design-contract home for this workflow; do not create a duplicate design tree or change AGENTS.md. |
| 8 | Low | Consistency | Kimi L2; GLM L2 | Approved architecture/specification are listed as prerequisites although this Sprint corrects them. | Accept | The wording is circular even though the documents remain the current source inputs. | Rephrase the prerequisite as existing baselines scheduled for conformance correction in this Sprint. |
| 9 | Info | Consistency | Kimi I1 | Traceability already remains `specified` and contains no obsolete one-hot text. | Reject | No defect exists in current status vocabulary or one-hot wording. | Do not change status; only add the accepted planned regression-test mapping described in row 14. |
| 10 | Info | Clarity | Kimi I2 | `pytest` needs at least one collected test. | Accept | The Sprint already promises import and CLI-help tests, but the acceptance can be more explicit. | Keep the command and ensure bootstrap tests are collected in Python 3.10 and 3.12 CI. |
| 11 | High | Consistency | GLM H1 | A real RunPod environment record cannot be produced by the Sprint's GitHub Actions-only acceptance boundary. | Accept | The development-plan Phase 0 exit criterion requires CI; real CUDA, driver, GPU, and VRAM values require a later Pod boot. | Make M1-01 acceptance cover the versioned template and required-field/secret-free tests in GitHub Actions. Defer a populated environment record to M2-02 E0.5 RunPod execution and state that it is not M1-01 evidence. |
| 12 | Medium | Clarity | GLM M2 | The `--num-add 1/5` notation is ambiguous. | Accept | FR-DATA-005 and FR-MODEL-003 define distinct exact argv for A-D. | Replace shorthand with an explicit configuration table: A none; B add=1; C pid index 4 and add=1; D pid index 4 and add=5. |
| 13 | Medium | Consistency | GLM M3 | The test plan may pull FR-REP-002 serialized-contract fixtures forward from M1-02. | Partial | `scripts/validate_software_docs.py` already has representative schema checks, but the Python contract service and exhaustive fixtures belong to M1-02. | Limit new M1-01 tests to documentation consistency, package/CLI bootstrap, and environment-template validation; retain existing docs-validator schema checks without expanding the M1-02 contract suite. |
| 14 | Medium | Security | GLM M4 | Secret-free environment records lack an explicit acceptance test. | Accept | Architecture §9 and the development plan prohibit credentials in artifacts and logs. | Add a negative template test that rejects credential-shaped field names and verify CI configuration obtains secrets only from GitHub/RunPod secret stores. |
| 15 | Low | Maintainability | GLM L1 | Traceability does not name the integer-PID/one-hot regression tests precisely. | Accept | FR-DATA-005 and FR-MODEL-003 rows mention broader planned tests but not the Phase 0 contract guard. | Update only those planned-test cells with integer-PID ordering/flag and obsolete-one-hot regression coverage; keep status `specified`. |
| 16 | Low | Consistency | GLM L3 | New `ci.yml` may duplicate the existing Pages workflow. | Accept | `.github/workflows/docs.yml` builds and deploys on main, while M1-01 needs PR/push verification across Python versions. | State that `ci.yml` owns verification and `docs.yml` remains deployment-only; avoid adding a second deployment path. |
| 17 | Low | Requirement | GLM L4 | Dependency-lock artifacts are not named. | Accept | The plan requires a reproducible lock hash but only existing `requirements-docs.txt` is concrete. | Enumerate the Python CI lock and RunPod lock artifacts and require the RunPod record template to carry the RunPod lock SHA-256. |
| 18 | Info | Documentation | GLM I1 | Literature-dossier content should remain outside this Sprint. | Accept | Development-plan Phase 0 requires separating unrelated dossier work. | Add the literature dossier, its script, and its tests to the explicit out-of-scope list. |

## Needs Immediate Action

- Apply rows 1-4 and 6-8 to make the contract-correction scope implementation-ready.
- Apply rows 11-18 to align cloud evidence, tests, CI ownership, locks, security, traceability, and scope boundaries.
- Keep VitePress configuration unchanged unless implementation diagnostics reveal a real defect.

## Can Be Deferred

- A populated RunPod environment record is deferred to M2-02 E0.5 cloud execution; only its versioned template and validation belong to M1-01.
- Full FR-REP-002 Python validators and serialized-contract fixtures remain in M1-02.

## Final Status

The Sprint plan is conditionally accepted. Implementation may begin only after the accepted and partially accepted plan changes are applied.
