# Sprint M1-02 Deterministic Core and Contracts Review Confirm

**Reviewed Inputs**

- `project/3-Plan/sprint-m1-02-deterministic-core-and-contracts.md`
- `project/4-Reviews/sprint-m1-02-deterministic-core-and-contracts-review-by-opencode-go-kimi-k2.7-code.md` (generated by the configured DeepSeek fallback)
- `project/1-Requirement/FR-DATA-001-frozen-source-manifest.md`
- `project/1-Requirement/FR-DATA-006-deterministic-split.md`
- `project/1-Requirement/FR-REP-001-content-addressed-provenance.md`
- `project/1-Requirement/FR-REP-002-machine-readable-validation.md`
- `docs/software/architecture.md`
- `docs/software/specification.md`
- `schemas/run-record.schema.json`
- `schemas/split-manifest.schema.json`
- `schemas/prediction.schema.json`

**Review Date**

- 2026-07-17

## Overall Conclusion

The review identifies real ambiguity at the boundary between deterministic helpers and later dataset production. The Sprint can proceed after the accepted and partially accepted clarifications below are applied. Full jet identity construction and event-disjointness validation remain implementable in this Sprint as pure functions over caller-supplied identities and synthetic fixtures; reading or producing scientific ROOT/HDF5 feature arrays remains out of scope.

Only one review report was produced after Kimi and GLM timed out and Ark returned HTTP 400. The user explicitly authorized continuing when only one report is available. The fallback report says it contains 13 findings, but its table contains 14 actionable rows; all 14 are answered below.

## Decision Table

| No. | Severity | Type | Review Source | Original Comment Summary | Decision | Evidence | Follow-up Plan / Rejection Reason |
|---:|---|---|---|---|---|---|---|
| 1 | Critical | Requirement | Finding 1 | The Sprint omits binding specification cross-references. | Accept | `docs/software/specification.md` §§2.1-2.3, 6.2-6.4, 7, and 9 define the exact algorithms, CLI, lifecycle, schemas, and error taxonomy. | Add an authoritative-contract subsection and bind implementation to those sections plus architecture §§6.2-6.4 and 8. |
| 2 | Critical | Correctness | Finding 2 | Event overlap cannot be derived from source-manifest rows. | Accept | Source rows contain record ID, PFN, checksum, and size; specification §2.2 defines event disjointness over separately supplied `(record_id, run, lumi, event)` identities. | Separate PFN assignment validation from an in-memory event-assignment semantic validator. Defer integration with extracted data to M2-01 while testing the validator now with synthetic records. |
| 3 | High | Clarity | Finding 3 | Full jet identity needs event and jet coordinates unavailable in the source manifest. | Partial | Specification §2.3 defines a pure deterministic formatting function; it does not require this Sprint to read an extraction artifact. | Implement PFN hashing and full jet-ID assembly from validated caller-supplied scalar components, with synthetic tests. Defer extraction wiring, not the pure helper. |
| 4 | High | Clarity | Finding 4 | Artifact lifecycle responsibilities make `contracts.py` too broad. | Accept | Architecture separates contract validation from artifact publication, and the Sprint already has distinct contract and artifact tests. | Add `src/particleml/artifacts.py`; keep JSON Schema and semantic validation in `contracts.py`. |
| 5 | High | Test | Finding 5 | No concrete PFN golden vectors are named. | Accept | Exact known inputs and expected digests/buckets are needed to prevent self-referential tests. | Add at least three fixed ASCII/Unicode PFNs with independently recorded SHA-256 and modulo-10 buckets to the Sprint and tests. |
| 6 | Medium | Requirement | Finding 6 | M1-01 native PID correction may be incomplete. | Partial | The correction is present in `docs/software/specification.md` §4.3, architecture §5.2, and commit `df2b60e`; the concern is valid but no blocking pre-task remains. | Record the completed prerequisite evidence and update the stale M1-01 workflow state for the user's no-push decision. |
| 7 | Medium | Clarity | Finding 7 | “Windows sentinel semantics” is undefined. | Accept | Architecture §6.4 makes `COMPLETED.json` authoritative regardless of rename behavior. | Replace the phrase with explicit completion-marker, partial-directory, and no-overwrite behavior on Windows and POSIX. |
| 8 | Medium | Requirement | Finding 8 | `SplitConfig` and canonical identity input are undefined. | Accept | Specification exposes `build_split_manifest(canonical, config)` and schema 1.0.0 requires dataset, source, partition, subset, preprocessing, and semantic fields. | Define `SplitConfig` and a minimal canonical identity-inventory input contract. Permit `split build` to read only identity/count metadata, not physics feature tensors. |
| 9 | Medium | Consistency | Finding 9 | The existing CLI already returns an integer and translates it only in `entrypoint`. | Accept | `src/particleml/cli.py` already implements the required boundary pattern. | State that M1-02 extends the existing parser and preserves the established return-code convention. |
| 10 | Low | Documentation | Finding 10 | VitePress commands may fail because configuration could be missing. | Reject | `docs/.vitepress/config.mjs` exists, and M1-01 recorded successful local `pnpm test` and `pnpm docs:build` diagnostics. | Keep both repository-wide diagnostics; no deferral note is needed. |
| 11 | Low | Test | Finding 11 | CLI groups lack targeted verification commands. | Accept | FR-DATA-001, FR-DATA-006, and FR-REP-002 each name targeted behaviors. | Add targeted pytest selections for manifest, split, contract, artifact, and CLI exit-code behavior. |
| 12 | Low | Clarity | Finding 12 | Public types are not mapped to their source contracts. | Partial | The types are binding public vocabulary in specification §1; some serve later stages but are intentionally introduced as stable value types now. | Add a type-to-source/responsibility table and distinguish value-only types from M1-02 behavioral types. |
| 13 | Info | Documentation | Finding 13 | Delivery evidence format is undefined. | Accept | The no-push workflow needs reproducible local commit and diagnostic records without claiming formal verification. | Add workflow-state and delivery-evidence sections with report paths, commands, result summaries, commit status, deferrals, and remaining risks. |
| 14 | Info | Risk | Finding 14 | The guarded normalization behaviors are not enumerated. | Accept | FR-DATA-001 and specification §2.1 explicitly forbid trimming, lowercasing, URL decoding, protocol normalization, BOM acceptance, and newline conversion. | Enumerate those mutations in the risk control and corresponding negative tests. |

## Needs Immediate Action

- Apply rows 1-9 and 11-14 to the Sprint before implementation.
- Keep JSON Schema version 1.0.0 unchanged unless implementation proves an actual schema contradiction.
- Record the user's single-review and no-push overrides without advancing formal verification status.

## Can Be Deferred

- Integration of event identities with real extraction output belongs to M2-01.
- Integration of the identity-inventory reader with canonical HDF5 feature arrays belongs to M1-03.
- GitHub Actions evidence remains deferred until the user separately authorizes a push.

## Final Status

Accept after the confirmed Sprint edits are applied. Implementation must not start before those edits are complete.
